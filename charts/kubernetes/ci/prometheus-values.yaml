# yaml-language-server: $schema=../values.schema.json
prometheus:
  url:
    value: http://prometheus
  auth:
    url:
      value: "ignore"
    username:
      value: user1
    password:
      value: "pass"
