ArgoCD integration installed.

{{- if .Values.topology.enabled }}
✓ Topology "{{ .Values.topology.name }}" created
{{- end }}

{{- if .Values.canary.enabled }}
✓ Health check canary created
  {{- if .Values.canary.repositories.enabled }}
  - Repository checks enabled
  {{- end }}
  {{- if .Values.canary.clusters.enabled }}
  - Cluster checks enabled
  {{- end }}
{{- end }}
